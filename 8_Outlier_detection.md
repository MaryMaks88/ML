<a name="top"></a>

Уяви, що ти з друзями міряєшся зростом. 
Усі ви приблизно одного зросту: хтось трошки вищий, хтось нижчий. 
І тут до вас у чергу стає... жирафа. Або мураха.

Ось ця жирафа і мураха — це і є Outliers (Викиди). 
Це дані, які настільки сильно відрізняються від усіх інших, що вони просто "ламають" твої розрахунки.

Чому Викиди — це погано?
Якщо ти захочеш порахувати середній зріст вашої компанії разом із жирафою, 
вийде, що ваш середній зріст — 3 метри! Це ж неправда, ви просто діти. 
Робот (модель ML) так само плутається: він бачить жирафу і думає, що всі люди мають бути величезними.

Як ми шукаємо "Жираф" у даних?
Один із найпопулярніших способів — це Метод IQR (міжквартильний розмах). Уяви, що ми вишикували всі дані в чергу за зростом і:

Відрізали 25% найменших.
Відрізали 25% найбільших.

Те, що залишилося посередині — це наша "нормальна" коробка. Все, що занадто далеко від цієї коробки — це викиди.

Приклад коду: Знаходимо "Багатіїв" у кредитах
Давай знайдемо людей, чий дохід настільки великий, що він збиває з пантелику нашу модель.

```python
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

# 1. Подивимося на дані за допомогою "Ящика з вусами" (Boxplot)
plt.figure(figsize=(8, 5))
sns.boxplot(x=df['ApplicantIncome'])
plt.title("Пошук 'жираф' у доходах")
plt.show()

# 2. Математичний спосіб знайти межі (IQR)
Q1 = df['ApplicantIncome'].quantile(0.25)
Q3 = df['ApplicantIncome'].quantile(0.75)
IQR = Q3 - Q1

lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

print(f"Все, що вище {upper_bound}, — це викиди (багатії)")

# 3. Відфільтровуємо їх (залишаємо тільки нормальні дані)
df_clean = df[(df['ApplicantIncome'] >= lower_bound) & (df['ApplicantIncome'] <= upper_bound)]
```

Що ми робимо з викидами?
У нас є три варіанти:

Видалити їх: Просто вигнати "жирафу" з черги. (Але іноді багатії теж важливі!).

Замінити їх: Сказати жирафі: "Слухай, ти занадто висока, давай ми будемо вважати, що ти просто дуже висока людина (як баскетболіст)".

Використати RobustScaler: Це спеціальний інструмент у пайплайні, який просто ігнорує ці викиди при масштабуванні, щоб вони нічого не зламали.

[Нагору ↑](#top)
